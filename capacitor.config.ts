import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kartikyazhini.badmintonsplit',
  appName: 'Badminton Split',
  webDir: 'static',
  server: {
    androidScheme: 'https'
  }
};

export default config;
